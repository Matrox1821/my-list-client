import React, { Fragment } from "react";
import { Header } from "./Header";

export const Layout = () => {
    return (
        <Fragment>
            <Header />
            <main>Hola</main>
        </Fragment>
    );
};
